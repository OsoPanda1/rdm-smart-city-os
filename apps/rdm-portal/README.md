# rdm-portal

Este módulo representa el frontend actual (Vite + React) dentro de la nueva estructura monorepo de TAMV OS.

> Implementación actual: el código fuente activo sigue en `src/` del repositorio raíz mientras se completa la migración incremental.
